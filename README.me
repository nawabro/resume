# Resume Generator

This project generates a PDF version of a resume from an HTML file using Puppeteer.

## Dependencies

This project uses the following dependencies:

- [Node.js](https://nodejs.org/): The project is a Node.js application.
- [Puppeteer](https://pptr.dev/): Puppeteer is used to generate a PDF from the HTML file.

The HTML file also uses the following resources:

- `resume.css`: The main stylesheet for the resume.
- `Document_files`: A directory containing additional stylesheets or resources.
- `Document_files/fontawesome/css/all.css`: The [Font Awesome](https://fontawesome.com/) stylesheet, used for icons in the HTML file.

## Installation

To install and run this project:

1. Clone the repository: `git clone <repository-url>`
2. Navigate to the project directory: `cd <project-directory>`
3. Install Node.js: Download and install from the [official website](https://nodejs.org/)
4. Install the dependencies: `npm install`
5. Run the script: `node resume1.js`

This will generate a `resume.pdf` file in the project directory.

## Customization

You can customize the resume by editing the `resume.html` and `resume.css` files. The `resume1.js` file contains the script that generates the PDF.

Please replace `<repository-url>` and `<project-directory>` with the actual URL of your project and the name of your project directory.